# AlphaGenome MCP Server Environment Variables

# Required: Your AlphaGenome API key (get from Google DeepMind)
ALPHA_GENOME_API_KEY=your-api-key-here

# Optional: Output directory for storing predictions and visualizations (default: ./alphagenome_output)
MCP_OUTPUT_DIR=./alphagenome_output

# Optional: Server configuration for SSE mode
MCP_HOST=localhost
MCP_PORT=3000

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO 